#include "systemc.h"
#include "gpio.h"
#include "tb_gpio.h"


int main(int argc, char* argv[])
{
	/*Add8bits  add("Add");
	Testbench testbench("Testbench");
	sc_signal<sc_uint<32> > sa,sc;

	testbench.a(sa);
	testbench.c(sc);

	add.a(sa);
	add.c(sc);

	sc_trace_file *trace;

	trace = sc_create_vcd_trace_file("trace");

	sc_trace(trace, sa, "a");
	sc_trace(trace, sc, "c");

	sc_start(500, SC_NS);

	sc_close_vcd_trace_file(trace);*/

	return 0;
}
